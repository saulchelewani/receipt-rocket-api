[pytest]
asyncio_mode = auto

# Set the default fixture loop scope to function
asyncio_default_fixture_loop_scope = function

# Disable the specific asyncio deprecation warning
filterwarnings =
    ignore::pytest.PytestDeprecationWarning:asyncio
    ignore:asyncio_default_fixture_loop_scope is unset:DeprecationWarning:pytest_asyncio.plugin
